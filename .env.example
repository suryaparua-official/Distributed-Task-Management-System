# Root environment file — loaded automatically by docker-compose.yml
# Copy to .env and customise before running in production.

# JWT secret shared by user-service and task-service.
# MUST be changed to a long random string before any real deployment.
JWT_SECRET=taskmanager_dev_secret_change_in_production
